server:
  port: 8082

spring:
  application:
    name: appointment-service

  datasource:
    url: jdbc:postgresql://postgres:5432/medirag_db?currentSchema=appointment
    username: medirag
    password: medirag123
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        default_schema: appointment
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: false

  data:
    redis:
      host: redis
      port: 6379

  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME:vedarthambilkar@gmail.com}
    password: ${MAIL_PASSWORD:difnnpttwkzsijtp}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

jwt:
  secret: "3f8a2b1c9d4e7f0a1b2c3d4e5f6a7b8c9d0e1f2a3b4c5d6e7f8a9b0c1d2e3f4"

cache:
  ttl-minutes: 10

---
spring:
  config:
    activate:
      on-profile: local
  datasource:
    url: jdbc:postgresql://localhost:5432/medirag_db?currentSchema=appointment
  data:
    redis:
      host: localhost